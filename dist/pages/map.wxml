
  <view class="body">
    <map id="myMap" markers="{{markers}}" polyline="{{polyline}}" longitude="{{pointLng}}" latitude="{{pointLat}}" scale="{{scale}}" show-location="{{true}}" style="width: 100%; height: 100%;">
                            <!-- 菜单 -->
                    <cover-view class="cover-view-conter">
                      <cover-view class="cover-down-img" bindtap="handleMyposition">
                        <cover-view class="cover-map-image">
                          <cover-image src="../images/map-nav-icon.png"/>
                        </cover-view>
                        <cover-view>我的位置</cover-view>
                    </cover-view>
                      <cover-view class="cover-down-img" bindtap="hotellocation">
                        <cover-view class="cover-map-image">
                          <cover-image src="../images/map-hotel-icon.png"/>
                        </cover-view>
                        <cover-view>酒店位置</cover-view>
                      </cover-view>
                    </cover-view>
                  </map>
  </view>